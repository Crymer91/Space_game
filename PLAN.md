# План реализации to-do.md (переработанный 2026-09-07)

> **Ключевое изменение механик:** монеты больше **нельзя** тратить во время матча.
> В игре **две раздельные экономики** и две раздельные валюты.

---

## Экономика (общая схема)

### Валюта 1 — Монеты (магазин / Ангар)
- Выпадают из метеоритов/комет и в матче, и в мультиплеере.
- Накапливаются в **банке игрока**.
- Трата **только в магазине/Ангаре** (между матчами).
- **Solo и мультиплеер — раздельные банки** (отдельные поля у игрока).
- 1-3 монеты за метеорит/комету.
- **1-й апгрейд = 10 монет, далее цена ×2 каждый уровень** (10, 20, 40, 80, 160...).

### Опыт / уровень / экспа (внутри матча, rogue-like)
- **Энергия (экспа)** — внутренний ресурс-прогресс, **не тратится**.
- Сферы энергии выпадают из врагов/комет/астероидов прямо в бою и накапливаются.
- Достиг порога → **уровень ↑** → показывается **бесплатный выбор 1 из 3 случайных карточек**.
- Игрок выбирает карточку → применяется бонус. Энергия при этом **не списывается**.
- Каждый следующий уровень — **дороже** (порог растёт, ×2).

> Уточнено (2026-09-07): выбор карточки **бесплатный** при достижении уровня; энергия не тратится, только накапливается как прогресс.

---

# ЗАДАЧИ

## ЧАСТЬ А. Каркас экономики

### А1. Разделение валют и банка
- [ ] В `shared/world.js`: у игрока добавляется `energy` (матчевая валюта) и `exp`/`level`.
- [ ] В `src/db.js`: раздельные поля банка монет: `coinsSolo` и `coinsMulti` (+ существующий `bestScore`).
- [ ] `upsertPlayer`/`submitScore` сохраняют монеты раздельно по режиму.
- [ ] В матче монеты больше **не тратятся** на апгрейды — `buyUpgrade` для монет удаляется/отключается.

**Файлы:** `shared/world.js`, `src/db.js`, `src/events.js`, `shared/balance.js`

### А2. Матчевый ресурс «Энергия» + дроп
- [ ] Новый тип дропа `energy` (энергетические сферы) выпадает из врагов/комет/астероидов.
- [ ] В `BALANCE` — параметры энергии: магнит, скорость, время жизни, объём за один дроп.
- [ ] Сферы ведут себя как монеты (магнит, подбор), но добавляют `energy` а не `coins`.
- [ ] Отображение энергии в HUD.

**Файлы:** `shared/world.js` (spawnEnergy, updateEnergy), `shared/balance.js`, `public/js/render.js`, `public/index.html`

### А3. Rogue-like: экспа → уровень → бесплатная карточка
- [ ] `energy` (экспа) накапливается, **не тратится** — просто прогресс.
- [ ] Порог следующего уровня растёт (×2). При достижении порога — **уровень ↑**.
- [ ] Генерируются **3 случайные карточки** из пула апгрейдов (урон, скорострельность, жизнь, скорость, магнит экспы, броня и т.д.).
- [ ] Клиент: UI-оверлей «Выберите карточку (1 из 3)», пауза, **бесплатный выбор**.
- [ ] Сервер (multi) и локальный мир (solo): применяют выбранную карточку без списания экспы.
- [ ] Механика работает и в solo, и в multi (симуляция общая).

**Файлы:** `shared/world.js`, `shared/balance.js`, `public/js/main.js`, `public/js/render.js`, `public/index.html`, новый `public/js/cards.js`

---

## ЧАСТЬ Б. Модули и способности (переработанные ответы Q3, Q4)

### Б1. Модули: разблокировка → активация → усиление
> Ответ Q3: модуль с босса в 1-й раз лишь **разблокирует** его в магазине.
> Дальше: **активация** (чтобы заряды падали с врагов) и **улучшение** (чаще падают, мощнее).

**Штатные модули:** ракеты (с Фантома), лазер, мины, броня.

- [ ] В `BALANCE.upgrades` — новая структура модулей: `unlockCost`, `activateCost`, `upgradeCosts[]`.
- [ ] `src/db.js` — у игрока поля: `modulesUnlocked: {}`, `modulesActive: {}`, `moduleLevels: {}` (по каждому модулю), раздельно solo/multi.
- [ ] Дроп зарядов модуля с врагов только если модуль **активен**.
- [ ] Мощность зависит от уровня модуля.
- [ ] **Модуль «Ракеты»** — разблокируется получением с Фантома (Q4). Только после активации ракеты падают с врагов.

**Файлы:** `shared/balance.js`, `shared/world.js`, `src/db.js`, `public/js/hangar.js`

### Б2. Магазин / Ангар (UI)
- [ ] Экран «Ангар» в главном меню:
  - [ ] Раздел **базовые характеристики** (карточки усиления — Q1): скорость, магнит, урон и т.д. Покупка за монеты, улучшение коэффициентов карточек (1%→3%→5%→10%→15%).
  - [ ] Раздел **модули** (ракеты, лазер, мины, броня): разблокировка → активация → улучшение.
  - [ ] Раздел **косметика** (скины корабля, цвета, эффекты) за монеты.
- [ ] Отображение банка монет (solo/multi — раздельно, переключатель).

**Файлы:** `public/js/main.js`, новый `public/js/hangar.js`, `public/index.html`, `public/style.css`

### Б3. Способности одноразовые (обновление миров)
- [ ] Каждая способность теперь — «модуль» с запасом `ammo` (одноразовых зарядов), пополняется дропом с врагов.
- [ ] Убран «бесплатный» реген брони по таймеру (`armorRegenAt`) — броня теперь модуль.
- [ ] Обновить `buyUpgrade` / внутренние покупки под новую систему зарядов.

**Файлы:** `shared/world.js`, `shared/balance.js`

---

## ЧАСТЬ В. Боссы и новые противники

### В1. Боссы — 3 уровня × 3 фазы (ответ Q5)
> Простой босс — 1 фаза, усиленный — 2 фазы, сильнейший — 3 фазы. Возвращаются в новых волнах с усилениями.

- [ ] `BALANCE.bosses.types` — по каждому боссу 3 варианта: базовый (`dreadnought`), `+` (2 фазы), `++` (3 фазы).
- [ ] Поле `boss.phase` (1..3). Фаза 1 включается на старте, далее по снижению HP.
- [ ] Расшифровка фаз:
  - **Дредноут** (`+`): стандарт → очереди по 3 → щит пополняемый монетами; (`++`) начало с фазы 2.
  - **Фантом** (`+`): стрельба+мины → телепорт+мины; (`++`) + клонирование.
  - **Левиафан** (`+`): круговой залп → спираль+ускорение; (`++`) + лазерные лучи.
- [ ] **Звук и визуал смены фаз** (обязательно по Q5): эффект, объявление, звуковой сигнал.
- [ ] Волны `waves.list` — усиленные версии боссов в поздних волнах.

**Файлы:** `shared/balance.js`, `shared/world.js` (updateBosses), `public/js/render.js`, `public/js/main.js`

### В2. Босс-сигнал-гудок + предупреждение (to-do #11)
- [ ] За 2с до появления босса — **гудок-волна**, выталкивающая игрока и объекты (`forcePush`).
- [ ] На экране — **мигающие значки** с той стороны, где появится босс (расширить существующий `fx: warning` на серию иконок).
- [ ] Звук: двойной низкий гудок.

**Файлы:** `shared/world.js` (forcePush, spawnBoss), `public/js/render.js`, `public/js/main.js`

### В3. Дредноут: очереди + пополняемая броня (to-do #4)
- [ ] Дредноут в фазе 2+ стреляет **очередями по 3 залпа**.
- [ ] Броня босса пополняется **подбором монет** (как у бронированных врагов).
- [ ] Урон сначала по броне, потом по HP. Полоска брони на UI.

**Файлы:** `shared/balance.js`, `shared/world.js`, `public/js/render.js`

### В4. Новые противники (3 типа, to-do #3)
- [ ] **Armored (бронированный):** броня = +100% HP; берёт 1 монету → +10% брони; старается не опускать броню ниже 30%; можно убить если не пополнил.
- [ ] **Burst (очередной стрелок):** быстрая серия выстрелов (интервал ~0.1с), пауза между очередями 2с. Усиливается, подобрав монеты.
- [ ] **Orbital (орбитальный):** держится на орбите (не строго), высокая скорость, **уничтожает метеориты которые мешают**, может столкнуться с игроком (урон).

**Файлы:** `shared/balance.js` (enemyKinds), `shared/world.js` (SPAWNERS + update), `public/js/render.js`

---

## ЧАСТЬ Г. Локализация спавна (to-do #10)

### Г1. Спавн не в секторе игрока
- [ ] Делим арену на 4 квадранта.
- [ ] `safeSpawnZone(world)`: определяет квадрант игрока; спавн в 3 других.
- [ ] Мультиплеер: 2 игрока → спавн в свободные квадранты (не занятые ни одним).
- [ ] Применение: `spawnAsteroid`, `spawnComet`, `spawnEnemy`.
- [ ] Боссы: спавнятся с кромки, но не с той стороны, где игрок.

**Файлы:** `shared/world.js`

---

## ЧАСТЬ Д. Ракеты (to-do #8)

### Д1. Переработка ракет
- [ ] Стандартные ракеты **по умолчанию отсутствуют**.
- [ ] Ракеты — модуль, **разблокируется с Фантома** (Q4), после активации в магазине падают с врагов.
- [ ] **Снижена скорость** движения: `missile.maxSpeed` и `accel` уменьшаются.
- [ ] **Разрушаются от выстрелов** (пули) — `detonateMissile` при попадании пули в ракету.
- [ ] Точное число `maxAmmo`, `pack` и др. в `BALANCE.missile`.

**Файлы:** `shared/balance.js`, `shared/world.js`, `public/js/render.js`

---

## ЧАСТЬ Е. Рейтинги (to-do #6, #7)

### Е1. Рейтинговая таблица — разделы
> Ответ Q2: все рейтинги отдельными разделами. Ответ Q7: solo и multi раздельно.

- [ ] Разделы: **Solo (по очкам)**, **Multi (по очкам)**, возможно **по монетам** (банк).
- [ ] `src/db.js`: `getLeaderboard(db, mode, playerId)` — позиция + 4 соседних (2 выше/2 ниже).
- [ ] `getTopPlayers(db, mode, limit)` — топ-10, топ-100.
- [ ] Сокет-ивенты `leaderboard:rank`, `leaderboard:top` с параметром `mode`.

**Файлы:** `src/db.js`, `src/events.js`, `public/js/main.js`

### Е2. UI рейтинга
- [ ] Экран результатов: «Ваш ранг: #N» + 5 строк (2 выше, я, 2 ниже).
- [ ] Кнопка «Рейтинг» в главном меню → оверлей с разделами и переключателем топ-10/топ-100.
- [ ] Подсветка текущего игрока.

**Файлы:** `public/index.html`, `public/style.css`, `public/js/main.js`

---

## ЧАСТЬ Ж. Курсор (to-do #1)

> **СТАТУС: ✅ ВЫПОЛНЕНО (2026-09-07)**

### Ж1. Курсор не теряется
- [x] `pointer-events: none` для DOM-оверлеев (HUD, upgradeBar, announce, hint) — уже было в CSS, проверено.
- [x] `cursor: none` на игровом экране — `style.css` `#gameScreen { cursor: none }`.
- [x] Усилить контраст кастомного прицела (тёмная подложка + свечение линий + яркая точка) — `render.js`.
- [x] Проверить, что не ломаются кнопки апгрейдов — `npm test` зелёный (11/11).

**Файлы:** `public/style.css`, `public/js/render.js`, `public/index.html`

---

# Порядок реализации (рекомендуемый)

> Статусы: ⬜ не начата · 🚧 в работе · ✅ выполнена

| # | Задача | Сложность | Влияет | Статус |
|---|--------|-----------|--------|--------|
| 1 | Ж1 Курсор | Низкая | — | ✅ |
| 2 | Г1 Спавн не в секторе | Средняя | — | ⬜ |
| 3 | А1 Разделение валют и банка | Средняя | — | ⬜ |
| 4 | А2 Энергия + дроп | Средняя | А1 | ⬜ |
| 5 | Д1 Переработка ракет | Средняя | А1 | ⬜ |
| 6 | Б1 Модули (разблокировка/активация) | Высокая | А1 | ⬜ |
| 7 | Б3 Способности одноразовые | Средняя | Б1 | ⬜ |
| 8 | А3 Rogue-like карточки | Высокая | А2 | ⬜ |
| 9 | В4 Новые противники | Высокая | Г1 | ⬜ |
| 10 | В3 Дредноут броня+очереди | Высокая | В4 | ⬜ |
| 11 | В1 Боссы 3 уровня | Очень высокая | В3 | ⬜ |
| 12 | В2 Сигнал-гудок | Средняя | — | ⬜ |
| 13 | Б2 Ангар (UI) | Высокая | Б1 | ⬜ |
| 14 | Е2 Рейтинг UI | Средняя | Е1 | ⬜ |
| 15 | Е1 Рейтинг сервер | Средняя | А1 | ⬜ |

---

# Решённые уточнения (закрыты 2026-09-07)

1. **Экспа/энергия** — накапливается как прогресс, **не тратится**. Выбор карточки **бесплатный** при достижении уровня; каждый уровень дороже (порог ×2).
2. **Монеты** выпадают и в solo, и в multi; банки раздельные. Карточки применяются и в solo, и в multi.
3. **Карточки в мультиплеере** — тот же механизм «экспа → уровень → бесплатная карточка», симуляция общая.
